# This file is executed on every boot (including wake-boot from deepsleep).
# Kept as the MicroPython stock default - the app itself is started from
# main.py (see README.md's "Deploy to the device" section).
import esp
# esp.osdebug(None)
